# Training pipeline

This folder is intentionally separate from the runtime API. Before training, provide a legitimate medical dataset and configure the class list in the YAML config.

## Example config

```yaml
classes:
  - class_1
  - class_2
  - class_3
```

Replace the placeholders with your actual labels.
